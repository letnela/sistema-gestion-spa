import{useState}from'react';import{useNavigate,useSearchParams,Link}from'react-router-dom';import{useAuth}from'../auth/AuthContext';import{errorMessage}from'../api/http';import toast from'react-hot-toast';import{Scissors}from'lucide-react';
export function LoginPage(){
 const[correo,setCorreo]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const{login}=useAuth();const nav=useNavigate();const[params]=useSearchParams();const next=params.get('next');
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);try{await login(correo,password);nav(next&&next.startsWith('/cliente')?next:'/')}catch(e){toast.error(errorMessage(e))}finally{setBusy(false)}};
 return <div className="grid min-h-screen place-items-center bg-gradient-to-br from-violet-950 via-slate-950 to-fuchsia-950 p-4"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><div className="mb-8"><div className="mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-violet-600 text-white"><Scissors/></div><h1 className="text-3xl font-bold text-slate-900">Bienvenido</h1><p className="mt-2 text-slate-500">Ingresa al sistema de gestión del salón.</p>{next&&<p className="mt-2 text-sm text-violet-600">Al ingresar te llevamos directo a lo que estabas viendo.</p>}</div><label className="label text-slate-700">Correo</label><input className="field mb-4" type="email" value={correo} onChange={e=>setCorreo(e.target.value)} placeholder="tu@correo.com" required/><label className="label text-slate-700">Contraseña</label><input className="field mb-6" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" required/><button className="btn-primary w-full" disabled={busy}>{busy?'Ingresando...':'Iniciar sesión'}</button><p className="mt-5 text-center text-xs text-slate-400">¿Olvidaste tus credenciales? Revisa el README del proyecto.</p><Link className="mt-4 block text-center text-sm text-violet-600" to={`/registro-cliente${next?`?next=${encodeURIComponent(next)}`:''}`}>Crear cuenta como cliente</Link></form></div>}
