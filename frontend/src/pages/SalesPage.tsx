import{Link}from'react-router-dom';import{useState}from'react';import{useQuery}from'@tanstack/react-query';import{http}from'../api/http';import{Pagination}from'../components/Pagination';import{StatusBadge}from'../components/StatusBadge';export function SalesPage(){const[page,setPage]=useState(1);const{data}=useQuery({queryKey:['sales',page],queryFn:async()=>{const{data}=await http.get('/sales',{params:{pagina:page,tamanio:10}});return data}});return <div className="space-y-5"><div className="flex items-center justify-between"><div><h1 className="page-title">Ventas</h1><p className="text-slate-500">Comprobantes, pagos y anulaciones.</p></div><Link className="btn-primary" to="/pos">Nueva venta</Link></div><div className="grid gap-4 sm:grid-cols-3"><div className="card"><p className="text-sm text-slate-500">Ventas listadas</p><p className="text-2xl font-bold">{data?.total||0}</p></div><div className="card"><p className="text-sm text-slate-500">Página actual</p><p className="text-2xl font-bold">{page}</p></div><div className="card"><p className="text-sm text-slate-500">Operación</p><p className="text-lg font-bold text-violet-600">Punto de venta conectado</p></div></div><div className="table-wrap"><table><thead><tr><th>Comprobante</th><th>Fecha</th><th>Cliente</th><th>Total</th><th>Estado</th></tr></thead><tbody>{(data?.items||[]).map((x:any)=><tr key={x.id}><td className="font-semibold">{x.numero_comprobante}</td><td>{new Date(x.created_at||x.fecha_venta).toLocaleString()}</td><td>{x.cliente_nombre||'Consumidor final'}</td><td>S/ {Number(x.total||0).toFixed(2)}</td><td><StatusBadge value={x.estado}/></td></tr>)}</tbody></table></div><Pagination page={page} total={data?.total_paginas||1} onChange={setPage}/></div>}
