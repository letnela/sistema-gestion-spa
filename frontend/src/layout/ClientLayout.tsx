import{NavLink,Outlet}from'react-router-dom';import{useAuth}from'../auth/AuthContext';import{Home,CalendarPlus,CalendarDays,UserRound,ReceiptText,LogOut,Scissors,ShoppingBag,Sparkles}from'lucide-react';
const links=[['/cliente','Inicio',Home],['/cliente/servicios','Servicios',Scissors],['/cliente/agendar','Agendar',CalendarPlus],['/cliente/tienda','Tienda',ShoppingBag],['/cliente/mis-citas','Citas',CalendarDays],['/cliente/compras','Compras',ReceiptText],['/cliente/perfil','Perfil',UserRound]] as const;
export function ClientLayout(){const{user,logout}=useAuth();return <div className="min-h-screen bg-[#faf8fc] text-slate-900 dark:bg-slate-950 dark:text-white"><header className="sticky top-0 z-40 border-b border-white/40 bg-white/90 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/90"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 sm:px-6"><NavLink to="/cliente" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-gradient-to-br from-violet-600 to-fuchsia-500 text-white shadow-lg"><Sparkles size={20}/></span><div><b className="font-serif text-lg tracking-tight">Éclat Beauty</b><p className="text-[10px] font-bold uppercase tracking-[.2em] text-slate-400">Beauty club</p></div></NavLink><nav className="hidden items-center gap-1 lg:flex">{links.map(([to,l,I])=><NavLink end={to==='/cliente'} key={to} to={to} className={({isActive})=>`flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-bold ${isActive?'bg-slate-950 text-white dark:bg-white dark:text-slate-950':'text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-900'}`}><I size={16}/>{l}</NavLink>)}</nav><div className="flex items-center gap-2"><div className="hidden text-right sm:block"><p className="max-w-40 truncate text-sm font-bold">{user?.nombre_completo}</p><p className="text-xs text-slate-400">Cliente</p></div><button className="icon-btn" onClick={logout} title="Cerrar sesión"><LogOut size={18}/></button></div></div></header><main className="mx-auto max-w-7xl px-4 py-5 sm:px-6 sm:py-8"><Outlet/></main><nav className="fixed inset-x-0 bottom-0 z-40 border-t bg-white/95 px-2 pb-[max(.5rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/95 lg:hidden"><div className="mx-auto grid max-w-lg grid-cols-5">{links.slice(0,5).map(([to,l,I])=><NavLink end={to==='/cliente'} key={to} to={to} className={({isActive})=>`flex flex-col items-center gap-1 rounded-xl py-1.5 text-[10px] font-bold ${isActive?'text-violet-600':'text-slate-500'}`}><I size={20}/><span>{l}</span></NavLink>)}</div></nav></div>}
