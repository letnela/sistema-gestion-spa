export function Pagination({page,total,onChange}:{page:number;total:number;onChange:(p:number)=>void}){return <div className="mt-4 flex items-center justify-end gap-2"><button className="btn-secondary" disabled={page<=1} onClick={()=>onChange(page-1)}>Anterior</button><span className="px-3 text-sm">Página {page} de {Math.max(total,1)}</span><button className="btn-secondary" disabled={page>=total} onClick={()=>onChange(page+1)}>Siguiente</button></div>}
